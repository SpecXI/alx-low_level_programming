#include "main.h"

/**
 * _abs - Computes the ablsolutr value of an integer
 * @c: The number to be computed
 * Return: Absolutr of numer or zero
 */

int _abs(int c)

{

	if (c < 0)
	{
	int abs_val;

	abs_vaal = c * -1;
	return (abs_val);
	}
	return (c);
}
